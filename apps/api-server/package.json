{
  "name": "api-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cross-env NODE_CONFIG_DIR=../../config nodemon",
    "prod": "cross-env NODE_CONFIG_DIR=../../config node src/app.js",
    "test": "echo from api-server"
  },
  "dependencies": {
    "shared": "^1.0.0",
    "jsonwebtoken": "^9.0.0"
  }
}
